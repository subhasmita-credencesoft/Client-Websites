# Canonical site URL used for SEO metadata, Open Graph tags, and sitemap.
# Set this to the production domain in Netlify's environment variables,
# e.g. https://bookonegroup.netlify.app
# Falls back to http://localhost:5000 in local development.
NEXT_PUBLIC_SITE_URL=https://your-production-domain.example.com