## Data Structure

- `content`
  Shared content used across multiple features or sections.
- `pages`
  Route-specific or page-model data used by page views.

Use `content` for reusable home/navigation/media datasets, and `pages` for page payloads such as detail pages, offers, gallery, info pages, and venue pages.
