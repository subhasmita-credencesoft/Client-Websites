# Lib

Shared non-visual application code.

- `data/`: content and page data
- `hotelmate/`: booking integration helpers and types
- `utils/`: pure utility helpers

Keep browser UI concerns in `components/`, not here.
