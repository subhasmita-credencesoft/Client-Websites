# Components

## Structure

- `features/`: route/domain modules
- `layout/`: global shell components
- `providers/`: app-wide client providers
- `ui/`: reusable presentational primitives

Prefer importing from local barrel files where available to keep feature boundaries clean.
