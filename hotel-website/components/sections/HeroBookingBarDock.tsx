import HeroBookingBar from "../features/HeroBookingBar";

export default function HeroBookingBarDock() {
  return (
    <section className="relative z-[30] -mt-[28vh] mb-[20vh] sm:-mt-[30vh] sm:mb-[22vh] lg:-mt-[32vh] lg:mb-[24vh]">
      <HeroBookingBar />
    </section>
  );
}
